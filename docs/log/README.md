# Research log

Dated entries, five lines each: what was tried, what happened, what's next.
This is where ideas and negative results live without polluting the README —
not a changelog (see [`../../CHANGELOG.md`](../../CHANGELOG.md) for that) and
not a status report (see [`../../STATUS.md`](../../STATUS.md) for that).

Example entry:

```
2026-08-24 — LongMemEval: bare 4.2%, Nous 0.0%. Diagnosis: adapter
vocabulary represents thematic relations, not atomic personal facts.
Decision: park LongMemEval, record the task-shape boundary. Next:
FNC-Bench calibration split.
```

No entries yet — this file was created 2026-08-25 as part of settling the
repo's structure before the first real benchmark sweep.
