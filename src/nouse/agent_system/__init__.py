"""Folder-driven agent system (ICM/Larynx pipeline).

See ../../ICM-agents.md for the governing contract. Agent identity and
policy live in folders read at runtime (NOUSE_AGENT_POLICY_DIR), not in
this package — this package only implements the generic stage machinery.
"""
